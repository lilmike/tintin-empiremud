#TICKER           {updateChecker}  {#script {newVersion} {git ls-remote origin -h refs/heads/master | cut -f1};#script {currentVersion} {git log | head -n 1 | cut -d " " -f2};#variable {currentVersion} {${currentVersion[1]}};#variable {newVersion} {${newVersion[1]}};#if {"$currentVersion" != "$newVersion"}{#showme {An update to tintin-empiremud is available. Pleas git pull, then reload any updated modules.};PLAY update}}                                                                                                                                                                                                                                                                                                                                                                                                                                                   {800}
#VARIABLE         {currentVersion}  {}
#VARIABLE         {oldVersion}  {}
#VARIABLE         {pulledChanges}  {false}
